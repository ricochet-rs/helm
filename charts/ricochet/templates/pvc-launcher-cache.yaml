{{- if and .Values.launcher.deployment.persistence.cache.enabled (not .Values.launcher.deployment.persistence.cache.existingClaim) -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "ricochet.fullname" . }}-launcher-cache
  labels:
    {{- include "ricochet.labels" . | nindent 4 }}
  {{- with .Values.launcher.deployment.persistence.cache.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
    - {{ .Values.launcher.deployment.persistence.cache.accessMode }}
  {{- if .Values.launcher.deployment.persistence.cache.storageClass }}
  {{- if eq .Values.launcher.deployment.persistence.cache.storageClass "-" }}
  storageClassName: ""
  {{- else }}
  storageClassName: {{ .Values.launcher.deployment.persistence.cache.storageClass | quote }}
  {{- end }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.launcher.deployment.persistence.cache.size }}
{{- end }}
