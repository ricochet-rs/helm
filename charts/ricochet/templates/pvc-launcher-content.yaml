{{- if and .Values.launcher.deployment.persistence.content.enabled (not .Values.launcher.deployment.persistence.content.existingClaim) -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "ricochet.fullname" . }}-launcher-content
  labels:
    {{- include "ricochet.labels" . | nindent 4 }}
  {{- with .Values.launcher.deployment.persistence.content.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
    - {{ .Values.launcher.deployment.persistence.content.accessMode }}
  {{- if .Values.launcher.deployment.persistence.content.storageClass }}
  {{- if eq .Values.launcher.deployment.persistence.content.storageClass "-" }}
  storageClassName: ""
  {{- else }}
  storageClassName: {{ .Values.launcher.deployment.persistence.content.storageClass | quote }}
  {{- end }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.launcher.deployment.persistence.content.size }}
{{- end }}
